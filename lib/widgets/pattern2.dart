import 'package:flutter/material.dart';

class Pattern2 extends StatelessWidget {
  const Pattern2({super.key});

  @override
  Widget build(BuildContext context) {
    return Positioned(
      top: 0,
      right: 0,
      child: Image.asset('../../assets/images/Pattern2.png'),
    );
  }
}
